name: Borrar Contenido de S3 Bucket

on:
  push:
    branches:
      - main

jobs:
  clear-s3-bucket:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set Up AWS CLI
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: AKIATWCEPH6F6WIQEKYD
        aws-secret-access-key: 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
        aws-region: us-east-1

    - name: Clear S3 Bucket
      run: aws s3 rm s3://arn:aws:s3:::finny-app-logs --recursive
